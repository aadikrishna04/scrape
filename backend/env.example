# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_KEY=your_supabase_service_role_key

# AI Configuration (for Gemini / LangChain)
GOOGLE_API_KEY=your_gemini_api_key
GEMINI_API_KEY=your_gemini_api_key

# GitHub OAuth (for MCP GitHub integration)
# IMPORTANT: Create an "OAuth App" (not a "GitHub App") at:
#   https://github.com/settings/developers → OAuth Apps → New OAuth App
# - Authorization callback URL: https://your-backend-host/api/integrations/github/oauth/callback
#   (e.g. http://localhost:8000/api/integrations/github/oauth/callback for local dev)
# After users authorize, the app must appear under:
#   GitHub → Settings → Applications → Authorized OAuth Apps
# If it appears under "Authorized GitHub Apps" instead, you created a GitHub App by mistake;
# create an OAuth App and use that Client ID and Client Secret to fix "Resource not accessible by integration".
GITHUB_OAUTH_CLIENT_ID=your_github_oauth_client_id
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret

# CORS
FRONTEND_URL=http://localhost:3000

# Google OAuth (for Gmail, Calendar, Drive)
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create a project (or select existing)
# 2. Go to "APIs & Services" → "Credentials" → "Create Credentials" → "OAuth client ID"
# 3. Choose "Web application"
# 4. Add authorized redirect URI: http://localhost:8000/api/integrations/google/oauth/callback
# 5. Enable required APIs in "APIs & Services" → "Library":
#    - Gmail API (for gmail)
#    - Google Calendar API (for google-calendar)
#    - Google Drive API (for google-drive)
# NOTE: For production, you'll need to verify your app with Google (can take weeks)
# For testing, you can use up to 100 test users without verification
GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your_secret

# ============================================
# INTEGRATION CREDENTIALS (Optional - per integration)
# ============================================

# Slack - https://api.slack.com/apps
SLACK_BOT_TOKEN=xoxb-...

# Brave Search - https://brave.com/search/api/
BRAVE_API_KEY=BSA...

# PostgreSQL
DATABASE_URL=postgresql://user:pass@host:port/db

# Notion - https://www.notion.so/my-integrations
NOTION_TOKEN=secret_...

# Linear - https://linear.app/settings/api
LINEAR_API_KEY=lin_api_...

# Jira - https://id.atlassian.com/manage-profile/security/api-tokens
JIRA_API_TOKEN=ATATT...
JIRA_EMAIL=you@company.com
JIRA_URL=https://yourcompany.atlassian.net

# Google (Gmail, Calendar, Drive) - https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=...apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-...

# Airtable - https://airtable.com/create/tokens
AIRTABLE_API_KEY=pat...

# Twilio - https://console.twilio.com/
TWILIO_ACCOUNT_SID=AC...
TWILIO_AUTH_TOKEN=...

# SendGrid - https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG...

# Stripe - https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...

# Discord - https://discord.com/developers/applications
DISCORD_BOT_TOKEN=...

# Trello - https://trello.com/app-key
TRELLO_API_KEY=...
TRELLO_TOKEN=...

# MongoDB - https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/db

# Redis - https://redis.com/try-free/
REDIS_URL=redis://user:pass@host:port

# AWS - https://console.aws.amazon.com/iam/
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_REGION=us-east-1

# Vercel - https://vercel.com/account/tokens
VERCEL_TOKEN=...
